# Kubernetes manifests for the BeamPay Rust backend API
# Applies: Deployment, ClusterIP Service, ConfigMap
# Target: docker images built by CI (see .github/workflows)

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: beampay-backend-config
  namespace: default
  labels:
    app: beampay-backend
data:
  RUST_LOG: "info"
  PORT: "8080"
  # Set DATABASE_URL and other secrets via a separate Secret resource or
  # an external secrets operator (e.g. AWS Secrets Manager CSI driver).

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: beampay-backend
  namespace: default
  labels:
    app: beampay-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: beampay-backend
  template:
    metadata:
      labels:
        app: beampay-backend
    spec:
      containers:
        - name: beampay-backend
          image: ghcr.io/fracverse/beampay-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: beampay-backend-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: beampay-backend-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: beampay-backend-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: beampay-backend
  namespace: default
  labels:
    app: beampay-backend
spec:
  type: ClusterIP
  selector:
    app: beampay-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
